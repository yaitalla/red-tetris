import React from 'react';
import PropTypes from 'prop-types';
import { field, flexDiv } from './style';


const PlayerMove = (props) => {
  let input
  return (
    <div style={flexDiv}>
    </div>
  )
}

// PlayerMove.propTypes = {
//   dispatch: PropTypes.func.isRequired
// }

export default PlayerMove;